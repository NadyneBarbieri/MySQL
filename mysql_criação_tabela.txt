create data base db_rh; /* criar banco de dados.
use db_rh;

/* criar tabela*\
create table tb_funcionarios(
id bigint auto_increment,
nome varchar(20) not null,
idedade int (3), 
endereço varchar(100),
rg int(15),
cpf int(15),
ativo boolean,
primary key (id)
);



insert into tb_funcionarios (nome, endereço, ativo, rg, cpf) values ("Nadyne","A",true,1,1)/* inserir informações na tabela *\

delete from tb_funcionarios where condição (1,2,3,5,6(apenas exemplos)); /* apagar varios dados de uma vez da tabela *\

select * from tb_funcionarios where salario >2000; */ mostrar salarios menor que 2000 na tabela *\

update tb_funcionarios set idedade= 60 /* atualizar dados da tabela *\
where id = 5; continuação

alter table tb_funcionarios rename column idedade to idade; /* alterar nome da tabela*\

alter table tb_funcionarios add salario int after cpf; /*adicionar colula na tabela*\

ALTER TABLE tb_funcionarios MODIFY COLUMN rg int(15); /* atualizar quantidade de caracter dentro da tabela*\

